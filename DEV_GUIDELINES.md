# 개발 지침 (Dev Guidelines)

> 이 문서는 대화 중 설정된 개발/코드 작업 지침을 백업한 것입니다.
> 프로젝트 디렉토리(예: `docs/DEV_GUIDELINES.md` 또는 리포지토리 루트)에 저장해 참조용으로 사용하세요.

## 목적

일관된 코드를 작성하고, 통신·데이터 처리 중에도 UI 응답성을 유지한다.

---

## 1. 코드 수정 전 승인 절차

코드를 직접 수정하기 전에는 반드시 먼저 작업 계획을 사용자에게 설명한다.

계획에는 다음 내용을 포함한다:
- 어떤 파일 또는 영역을 확인할지
- 어떤 방향으로 수정할지
- 수정 후 무엇으로 검증할지

작업 계획을 설명한 뒤에는 반드시 `진행할까요?`라고 물어본다.

사용자가 `진행` 또는 명확한 긍정/승인 의사를 답한 경우에만 직접적인 코드 수정, 파일 생성, 파일 삭제를 시작한다.

`진행`, `진행해줘`, `ㄱㄱ`, `응`, `ㅇㅋ`, `좋아`, `해줘`처럼 문맥상 작업 진행을 승인하는 답변은 승인으로 간주할 수 있다.

다만 답변이 애매하거나, 진행 승인인지 범위 변경 요청인지 명확히 알기 어려운 경우에는 직접 수정하지 않고 다시 확인 질문을 한다.

단, 사용자가 명시적으로 파일 생성이나 수정을 요청한 경우에도 이 승인 절차를 우선 적용한다.

---

## 2. 컨텍스트 압축/요약 복원 후 재개 규칙

같은 대화에서 컨텍스트 자동 압축이 발생했거나, 사용자가 `/압축` 등으로 직접 압축/요약을 요청한 뒤 이전 작업을 이어가는 경우에는 즉시 작업을 계속하지 않는다.

작업을 재개하기 전에 먼저 다음 내용을 짧게 요약한다.

- 현재 이해한 작업 목표
- 지금까지 완료된 내용
- 아직 남은 작업
- 앞으로 확인해야 할 파일 또는 문서
- 압축/요약 기준으로 계속 진행 가능한지 여부

그 다음 사용자에게 아래 문구로 확인한다.

```text
컨텍스트 압축 또는 요약 복원 이후의 이해를 기준으로 계속 진행해도 될까요?
진행하려면 `진행` 또는 명확한 긍정/승인 의사를 답해주세요.
```

사용자가 `진행` 또는 명확한 긍정/승인 의사를 답하기 전에는 코드, 문서, 설정 파일을 직접 수정하지 않는다.

압축/요약 내용보다 현재 파일 내용, 현재 소스 코드, 현재 프로젝트 문서를 우선한다.

---

## 3. 기본 개발 원칙

- 요구사항과 성공 기준을 먼저 확인한다. 불명확한 내용은 가정하지 말고 질문한다.
- 요청 범위에 필요한 코드만 수정한다. 주변 코드의 임의 리팩터링이나 추측성 기능 추가는 하지 않는다.
- 새 추상화보다 단순한 구현을 우선하고, 기존 모듈의 구조와 스타일을 따른다.
- 변경의 트레이드오프와 영향 범위(UI, 통신, 파일, 설정, 라이선스)를 기록한다.
- 완료 전에 빌드와 관련 테스트 또는 재현 절차로 변경 결과를 확인한다.

---

## 4. 코드 컨벤션

- C# 표준 명명 규칙을 따른다: 타입·메서드·공개 멤버는 `PascalCase`, 지역 변수·매개변수는 `camelCase`, private 필드는 `_camelCase`.
- Nullable 경고를 숨기지 않는다. `null`이 가능한 이유와 처리 위치를 코드로 명확히 한다.
- 메서드와 클래스는 한 가지 책임만 갖게 하며, 긴 이벤트 핸들러의 로직은 Presenter 또는 Service로 분리한다.
- `async void`는 UI 이벤트 핸들러에만 사용한다. 그 외 비동기 메서드는 `Task`를 반환하고 필요하면 `CancellationToken`을 받는다.
- 예외를 무시하지 않는다. 복구 가능한 예외는 처리하고, 나머지는 원인과 문맥을 포함해 기록하거나 상위로 전달한다.
- `IDisposable` 자원과 이벤트 구독은 소유 주체가 명확히 해제한다.
- 주석은 코드가 하는 일보다 제약, 이유, 트레이드오프를 설명할 때만 작성한다.
- 만약 주석이 필요하면, 한번 더 함수로 만들 수 없는지 확인한다.
- 최대한 구구절절한 주석은 적지 않는다.

---

## 5. 구조와 의존성

- View는 표시와 입력에 집중하고, 업무 로직은 Service에 둔다.
- Presenter/ViewModel는 Service 불러와 전체 로직을 수행하는 용도로 활용된다.
- 모듈 간에는 구체 타입보다 기존 인터페이스와 DTO 계약을 우선한다.
- 실시간 통신 경로와 UI 표시 경로를 분리한다. UI 지연이 수집·송수신을 막아서는 안 된다.

---

## 6. CFG 설정값 및 기능 (필수)

- 사용자 또는 모듈의 설정값을 새로 만들거나 변경하는 개발에는 **CFG 저장·복원 기능을 반드시 추가한다.**
- CFG에는 기능이 다시 열리거나 프로젝트를 다시 불러온 뒤에도 재현되어야 하는 설정값을 포함한다. 예: 화면 표시 옵션, 신호·채널 선택, 갱신 주기, 범위, 레이아웃 및 기능별 동작 옵션.
- 기존 CFG 계약과 `IConfigSnapshotProvider` 흐름을 우선 사용한다. 신규 설정을 메모리에만 보관하거나 화면 상태에만 의존하지 않는다.
- 설정값의 기본값, 유효 범위, 이전 CFG와의 호환성, 누락·손상된 CFG를 읽을 때의 동작을 정의한다.
- CFG 저장과 복원은 UI 스레드를 오래 점유하지 않도록 하고, 저장·복원 후 기능이 같은 상태로 동작하는지 확인한다.

---

## 7. 성능 필수 규칙

### 측정

- 최적화 전후를 동일한 데이터와 조건에서 측정한다. 측정값 없이 개선 효과를 단정하지 않는다.
- CPU, 메모리, GC, 처리량, Queue 길이, UI 응답 시간을 함께 확인한다.
- 1ms 보장이 필요한 작업은 일반 Windows Timer에 맡기지 않고 Hardware/Firmware 또는 검증된 Real-time Target에서 처리한다.

### 송수신 Hot Path

- 프레임 송수신과 파싱 경로는 **Zero Allocation을 목표**로 한다. 송수신 중 GC 발생은 지터와 프레임 처리 지연을 유발할 수 있다.
- 반복 실행 경로에서 새 배열·객체·문자열 생성, Boxing, LINQ, 캡처 람다, 문자열 기반 로그를 피한다.
- 버퍼는 가능한 한 사전 할당하거나 `Span<T>`, `Memory<T>`, `ArrayPool<T>` 등으로 재사용한다. 풀링한 버퍼의 소유권과 반환 시점을 명확히 한다.
- 부하 테스트에서 스레드별 할당량과 GC 발생 횟수를 측정한다. 불가피한 할당은 근거와 영향 범위를 기록한다.

### UI 스레드

- UI 스레드에는 화면 표시, 입력, Dialog, Control/Window 조작만 둔다.
- DB·파일·네트워크 I/O, CPU 계산, 대량 반복 처리는 UI 스레드에서 실행하지 않는다.
- UI 경로에서 `Thread.Sleep`, `Task.Wait()`, `.Result`, `GetAwaiter().GetResult()`를 사용하지 않는다.
- 백그라운드 작업에서 Control, Window, Dialog를 생성하거나 조작하지 않는다.

### 데이터와 화면 갱신

- 데이터 수집 주기와 화면 표시 주기를 분리한다.
- 고빈도 상태를 UI에 1:1로 전달하지 않는다. `Buffer → Snapshot → ViewModel/UI Apply` 흐름을 사용한다.
- 로그, Grid, 차트, 센서값은 Batch·Throttle·Dirty Flag 방식으로 갱신한다.
- UI 갱신 주기는 일반 실시간 표시에 `10~250ms`, 로그·Grid·상태판에 `300~1000ms`를 우선 검토한다. 더 짧아야 한다면 근거와 측정값을 남긴다.
- 값이 실제로 바뀐 경우에만 `PropertyChanged`를 발생시킨다.
- 반복적인 `Invoke`/`BeginInvoke` 대신 변경을 모아 한 번에 전달한다.
- Modal Dialog가 열려 있는 동안 메인 화면 갱신을 중지하거나 저빈도 모드로 전환한다.

### WinForms / WPF

- WinForms 대량 변경에는 `SuspendLayout`/`ResumeLayout`, 목록 변경에는 `BeginUpdate`/`EndUpdate`를 사용한다.
- `Paint`에서 I/O, 이미지 로딩, 대량 계산, GDI 객체 반복 생성을 하지 않는다.
- WPF 대량 목록은 가상화와 일괄 갱신을 검토한다. Converter, ValidationRule, DataTemplate은 가볍게 유지한다.
- `Image`, `Bitmap`, `Graphics`, `Stream`, `Timer`, 동적 Control 등은 반드시 해제한다.

---

## 8. 변경 완료 체크리스트

- [ ] 요구사항 밖의 변경이 없다.
- [ ] 설정값을 신설·변경한 경우 CFG 저장·복원 기능과 기본값·호환성 처리를 추가했다.
- [ ] UI 스레드 Blocking과 고빈도 UI 갱신이 없다.
- [ ] 송수신 Hot Path의 할당량과 GC 발생 여부를 확인했다.
- [ ] Queue 폭증, 이벤트 중복 구독, 자원 해제 누락을 확인했다.
- [ ] 오류·취소·종료 경로에서도 상태가 일관된다.
- [ ] 관련 빌드와 테스트 또는 재현 절차가 통과했다.
- [ ] 성능 변경은 전후 측정 결과를 남겼다.
