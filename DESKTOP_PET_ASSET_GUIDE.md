# DesktopPet 원본 기반 동작 이미지 제작 가이드

작성일: 2026-09-21 · 상태: 제작 전 규격 제안

목표는 원본 캐릭터의 생김새를 유지한 채 동작별 이미지를 만들고, 검수한 이미지를 WPF에서 재생하는 것이다. 행동 ID는 [동작 정의 문서](DESKTOP_PET_BEHAVIOR_SPEC.md)와 맞춘다.

현재 원본 캐릭터 이미지가 제공되지 않았고 저장소에서도 PNG·JPG·WebP 원본을 찾지 못했다. 이 문서 작성 시점에는 동작 이미지를 생성하지 않았다.

## 1. 어떤 방식으로 맡기면 되는가

원본 한 장을 제공하면 이미지 생성 도구에 그 이미지를 참조시켜 자세·표정·동작 변형을 제작할 수 있다. 먼저 동작별 대표 자세를 확인하고, 선택한 자세를 기준으로 연속 프레임을 만드는 순서를 제안한다.

**대표 자세 이미지와 애니메이션 프레임은 다르다.** 앉은 모습·걷는 모습·자는 모습 각 한 장은 상태를 보여줄 수 있지만, 자연스러운 걷기 애니메이션에는 발의 위상이 연결되는 여러 프레임이 필요하다.

생성 결과가 캐릭터 일관성, 정확한 프레임 간격, 투명 배경을 항상 만족하는 것은 아니다. 결과 검수와 필요한 재생성이 제작 과정에 포함된다. 프롬프트만으로 즉시 사용 가능한 스프라이트 시트를 보장하지 않는다.

## 2. 원본에서 먼저 고정할 것

| 항목 | 고정할 내용 |
|---|---|
| 정체성 | 얼굴, 눈, 귀, 꼬리, 몸 비율, 무늬, 장식 |
| 표현 | 선 굵기, 채색 방식, 그림자, 픽셀/일러스트 스타일 |
| 시점 | 원본에 맞춘 기본 시점 하나, 걷기에 필요한 방향 |
| 색 | 기본색·무늬·눈의 색을 문서로 기록 |
| 비대칭 | 한쪽 무늬나 장식 때문에 좌우 반전이 가능한지 |
| 발 기준 | 지면과 닿는 위치 및 전체 캔버스 내 크기 |

원본에 보이지 않는 측면·뒷면은 추정이 필요하다. 처음에는 원본 시점과 좌우 이동에 필요한 최소 방향만 제작한다. 원본을 여러 번 변형한 결과만 계속 참조하면 디자인이 달라질 수 있으므로 매 제작에 원본도 함께 사용한다.

## 3. 제작 순서

### A. 동작별 대표 자세 6장

| 파일 ID | 자세 요청 | 확인할 부분 |
|---|---|---|
| idle-key | 원본 시점으로 편하게 서 있음 | 얼굴·비율의 기준 |
| walk-key | 같은 캐릭터가 오른쪽으로 한 걸음 걷는 자세 | 다리 구조와 진행 방향 |
| rest-key | 편안하게 앉아 쉬는 자세 | 몸을 낮춰도 같은 캐릭터인지 |
| sleep-key | 눈을 감고 편하게 자는 자세 | 귀·꼬리·무늬 유지 |
| react-key | 쓰다듬어 기분 좋아하는 표정과 자세 | 새 손·인물 없이 반응 표현 |
| drag-key | 들어 옮겨지는 듯 몸과 발이 살짝 늘어진 자세 | 손·끈·집게 없이 펫만 표시 |

첫 제작은 idle-key와 walk-key 두 장으로 화풍과 형태를 확인한다. 방향이 맞으면 나머지 네 자세를 제작한다. 검수용 모음 이미지는 비교에 사용하고, 앱에 넣을 최종 이미지와 구분한다.

### B. 동작별 연속 프레임

아래 규격을 첫 세트로 제안한다. 원본이 사람형인지 동물형인지에 따라 걷기 자세의 세부 내용은 조정한다.

| 클립 ID | 프레임 수 | 프레임 시간 | 반복 | 프레임 제작 지시 |
|---|---:|---:|---|---|
| idle | 4 | 250ms | 예 | 편한 자세 → 미세한 들숨 → 눈 깜박임 → 기본 자세로 복귀 |
| walk | 6 | 125ms | 예 | 발 접지와 통과 자세가 이어지는 제자리 보행 한 주기 |
| rest | 2 | 500ms | 예 | 앉은 기본 자세 → 작은 호흡 변화 |
| sleep | 4 | 300ms | 예 | 수면 자세를 유지하며 느린 호흡 한 주기 |
| react | 4 | 150ms | 아니오 | 반응 시작 → 기쁨 → 기쁨 정점 → 안정 |
| drag | 1 | 100ms | 예 | 들린 자세를 유지하는 정지 프레임 |

총 21프레임이다. 좌우 별도 그림이 필요하면 추가 프레임이 생긴다. 반복 재생을 위해 마지막에 첫 프레임을 한 번 더 복제해 넣을 필요는 없다. 마지막에서 첫 프레임으로 이어지는 움직임을 확인한다.

걷기는 **이미지 안에서는 제자리 보행**, 화면 이동은 WPF가 담당한다. 프레임 안에서 캐릭터까지 좌우로 옮기면 이동이 중복되어 흔들려 보일 수 있다.

1프레임 Drag도 런타임에는 반복 클립으로 취급할 수 있지만 같은 이미지를 불필요하게 다시 할당하지 않는다.

### C. 규격 정리와 패키징

제작 원본과 최종 재생 파일을 구분한다. 생성 도구 출력은 검수 후 동일 크기·기준점에 맞춘다. 프레임별로 몸통 크기가 달라지지 않도록 같은 기준으로 정규화하고, 프레임마다 불투명 영역에 꽉 맞춰 확대하지 않는다.

초기 런타임은 **동작별 PNG 한 장에 프레임을 가로로 배치한 스트립**을 제안한다. 앱은 정의 파일의 크기로 잘라 사용한다. 이미지 생성 도구가 만든 임의 격자를 그대로 신뢰하지 않고, 검수한 프레임을 정확한 규격으로 패키징한다.

## 4. 최종 파일 규격

- 포맷: 실제 알파 채널을 가진 PNG. 체크무늬를 그려 넣은 배경은 투명 배경이 아니다.
- 프레임: 256×256 픽셀 정사각형.
- 공통 기준점: `(128, 232)` 픽셀. 발 또는 지면 접촉 기준이다.
- 기본 표시: 160×160 DIP. 이미지 픽셀 크기와 WPF 표시 크기를 분리한다.
- 모든 프레임에서 귀·꼬리·장식이 잘리지 않게 한다.
- 수면 자세도 같은 캔버스 크기를 유지한다. 높이가 낮아졌다고 확대하지 않는다.
- Drag의 기준점은 배치를 위한 공통 좌표로 유지하되 발을 그보다 위로 들어 올릴 수 있다.
- 외곽에 불필요한 배경·문자·프레임 번호·워터마크·새로운 소품을 넣지 않는다.
- 픽셀 아트 여부는 원본 스타일을 따른다. 원본이 일러스트면 임의로 픽셀화하지 않는다.

이 규격은 최종 앱용 출력 규격이다. 생성 도구의 출력 해상도를 256×256으로 보장한다는 뜻이 아니다.

```text
ArtSource/default/                 제작 보관용, 앱 배포 대상과 분리
  reference.png                    보존한 원본
  character-notes.md               색·비율·특징·반전 가능 여부
  prompts.md                       실제 사용한 제작 요청
  keyposes/                        대표 자세
  frames/                          검수한 개별 프레임

Assets/Pets/default/               앱에서 소비할 최종 파일
  character.json
  idle.png                         1024×256 / 4프레임
  walk.png                         1536×256 / 6프레임
  rest.png                         512×256 / 2프레임
  sleep.png                        1024×256 / 4프레임
  react.png                        1024×256 / 4프레임
  drag.png                         256×256 / 1프레임
```

경로와 파일은 아직 생성되지 않았다. 생성 결과는 선택 후 프로젝트 안에 복사하여 앱이 외부 생성 결과 폴더에 의존하지 않게 한다. 원본은 덮어쓰지 않는다.

## 5. 캐릭터 정의 파일 예시

다음 JSON은 좌우 반전 가능한 캐릭터를 가정한 계약 초안이다. 로더는 아직 없다.

```json
{
  "schemaVersion": 1,
  "id": "default",
  "frameSizePx": { "width": 256, "height": 256 },
  "anchorPx": { "x": 128, "y": 232 },
  "defaultFacing": "right",
  "allowMirror": true,
  "clips": {
    "idle": { "file": "idle.png", "frameCount": 4, "frameDurationMs": 250, "loop": true },
    "walk": { "file": "walk.png", "frameCount": 6, "frameDurationMs": 125, "loop": true },
    "rest": { "file": "rest.png", "frameCount": 2, "frameDurationMs": 500, "loop": true },
    "sleep": { "file": "sleep.png", "frameCount": 4, "frameDurationMs": 300, "loop": true },
    "react": { "file": "react.png", "frameCount": 4, "frameDurationMs": 150, "loop": false },
    "drag": { "file": "drag.png", "frameCount": 1, "frameDurationMs": 100, "loop": true }
  }
}
```

状態 ID는 해당 소문자 클립 ID로 매핑한다. 프레임 i의 잘라낼 영역은 `(i × 256, 0, 256, 256)`이다. 프레임 수·시간은 양수, 이미지 너비는 `frameCount × 256`, 높이는 256이어야 한다.

`allowMirror=false`인 캐릭터는 각 클립에 `leftFile`을 추가하여 왼쪽 스트립을 제공한다. 프레임 수·시간·크기는 오른쪽과 동일하게 유지한다. 왼쪽 파일이 없으면 양방향 스킨으로 승인하지 않는다. 반전은 펫 이미지에만 적용하고 메뉴·창 좌표에는 적용하지 않는다.

로더는 스킨 폴더 밖을 가리키는 경로, 알 수 없는 스키마, 잘못된 이미지 크기·프레임 수를 거부한다. 메모리 상한을 정하고 큰 파일을 무제한 로드하지 않는다. 필수 클립이 누락되면 스킨을 사용하지 않고 기본 스킨으로 복구한다. 기본 스킨도 없으면 내장 자리표시자를 사용한다.

## 6. 이미지 제작 요청문

### 공통 요청

```text
첨부한 원본을 기준으로 바탕화면 펫 앱에 사용할 동일 캐릭터를 제작한다.
원본의 얼굴, 눈 모양, 귀, 꼬리, 체형, 무늬, 장식, 색, 선과 채색 스타일을 유지한다.
바꿀 것은 아래에 지정한 자세와 표정뿐이다.
원본에 없는 장식이나 팔다리, 사람, 손, 문자를 추가하지 않는다.
캐릭터 전체가 보이고 귀와 꼬리가 잘리지 않게 한다.
투명한 배경을 사용한다. 배경에 체크무늬를 그리지 않는다.
카메라 거리와 몸통의 크기를 일정하게 유지한다.
최종 사용 크기가 작으므로 실루엣과 표정을 명료하게 표현한다.

이번 결과물: [대표 자세 1장 또는 특정 동작의 특정 프레임]
동작: [idle / walk / rest / sleep / react / drag]
변경할 자세: [구체적인 지시]
기준 방향: [원본에 맞춘 시점과 방향]
반드시 유지할 특징: [원본 확인 후 작성]
```

### 걷기 프레임 추가 요청

```text
원본은 캐릭터 정체성 기준, 선택한 walk-key는 걷기 시점과 비율 기준으로 사용한다.
오른쪽을 향한 제자리 걷기의 [N]번째 프레임을 제작한다.
해당 위상은 [접지 / 체중 이동 / 발 통과 / 반대 발 접지 등]이다.
캐릭터가 캔버스 안에서 좌우로 이동하거나 몸통 크기가 바뀌지 않게 한다.
원본의 팔다리 수와 구조를 유지한다.
앞뒤 프레임과 이어지는 발 위치를 유지한다.
```

실제 프레임 위상은 원본의 이족·사족 구조를 보고 정한다. 걷기 한 장을 복제해 좌우로 흔드는 효과만으로 걷기 클립을 대체하지 않는다.

동작별로 별도 요청을 사용하되, 각 요청에 원본과 선택한 대표 자세를 함께 제공한다. 첫 결과부터 전체 프레임을 대량 생성하기보다 Idle·Walk를 먼저 완성하고 나머지를 확장한다.

## 7. 사용 전 검수

| 검수 | 통과 기준 |
|---|---|
| 동일 캐릭터 | 모든 자세에서 얼굴·무늬·귀·꼬리가 유지됨 |
| 알파 | 밝은 배경과 어두운 배경 모두 사각 배경·테두리 오염이 없음 |
| 크기 | 프레임마다 몸 크기가 튀지 않음 |
| 기준점 | 발 접촉 기준이 튀지 않고 전환 시 전체 그림이 밀리지 않음 |
| 루프 | 마지막 → 첫 프레임에서 갑자기 끊기지 않음 |
| 걷기 | 접지한 발과 화면 이동이 크게 어긋나지 않음 |
| 방향 | 좌우 반전 시 비대칭 특징이 틀어지지 않거나 별도 이미지 제공 |
| 실제 크기 | 80·160·240 DIP에서 실루엣과 표정이 읽힘 |
| 입력 | 보이는 펫을 클릭·드래그할 수 있고 주변 투명 여백의 클릭 동작을 확인함 |
| 계약 | JSON 프레임 수·크기·시간이 실제 파일과 일치함 |

검수용으로 프레임 모음과 반복 재생 미리보기를 함께 만든다. 모음 이미지만 보고 애니메이션 완료로 판단하지 않는다. 최종적으로 WPF 창에서 재생하여 타이밍과 화면 이동을 확인한다.

## 8. 다음 제작에 필요한 입력

필수 입력은 **기준으로 삼을 원본 캐릭터 이미지 한 장**이다. 이미지 첨부 또는 저장소 안의 파일 경로로 받을 수 있다.

추가 지정이 없다면 원본 스타일 유지, 투명 배경, 한 캐릭터, 최소 방향, 이 문서의 6개 동작을 기준으로 진행한다. 비대칭 무늬와 시점은 원본 확인 후 판단한다. 원본 없이 별개의 캐릭터를 임의 생성하여 원본 기반 결과라고 취급하지 않는다.
